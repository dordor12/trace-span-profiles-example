server:
  port: 8080

spring:
  application:
    name: ${OTEL_SERVICE_NAME:demo-app}

# Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example: DEBUG
